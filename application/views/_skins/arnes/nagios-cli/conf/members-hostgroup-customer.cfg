

{if count( $c.hostnames )}
define hostgroup {
        hostgroup_name  six-{$c.shortname}-routers
        alias           SIX {$c.name} Routers
        members         {implode( $c.hostnames, ',' )}
}
{/if}
