

{if count( $c.hostnames )}
define hostgroup {
        hostgroup_name  {$ixp->getShortname()|lower}-{$c.shortname}-routers
        alias           {$ixp->getShortname()} - {$c.name} Routers
        members         {getMembers list=$c.hostnames}

}
{/if}
